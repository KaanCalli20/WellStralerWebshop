using System;
using System.Resources.NetStandard;
using Microsoft.Extensions.Localization;


namespace WellStralerWebshop.Models.Domain
{
    public class Taal
    { /*
        public string currentTaal { get; private set; }
        public string defaultTaal = "NL";
        public string resxFile = @".\NL.resx";

        using (ResXResourceReader resxReader = new ResXResourceReader(resxFile)){
    }+

        public void setTaal(string taal)
        {
            currentTaal = taal;
        }

        public string getWoord(string key)
        {
            Console.WriteLine(resourceManager.GetString("Login"));
            return resourceManager.GetString("Login");
        }*/
    }
}
